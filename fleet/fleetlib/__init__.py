"""Shared, dependency-free building blocks for the Cashbot fleet.

Everything in this package uses only the Python standard library so a fleet can
be zipped up and run on any machine with python3 and nothing else installed.
"""

__version__ = "1.0.0"
